1. 在转换为矢量数据前，经常需要用r.thin模块将栅格行够着到单像素宽度，然后用r.to.vect模块进行矢量化并且默认设置为线(line)特征类型；

2. 没有拓扑的矢量图并不被大多数模块支持，且应该被认为是用来转化为栅格图或用v.out.ascii导出为ASCII文件的临时地图；

3. 当分辨率差别较大或栅格数据有较大的缺口(gap)时，r.resamp.interp并不会起作用，因为所用邻域范围大小的限制，如3*3邻域，此时仅以用样条差值。在grass中，栅格和矢量的样条差值模块分别是r.resamp.rst和v.surf.rst。

4. 